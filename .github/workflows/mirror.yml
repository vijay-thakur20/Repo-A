name: Sync repo A to repo B

on:
  push:
    branches:
      - main

jobs:
  sync:
    if: github.repository == 'vijay-thakur20/Repo-A'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo A
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Push to repo B
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: |
          git remote add mirror https://x-access-token:${TOKEN}@github.com/vijay-thakur20/Repo-B.git
          git push mirror main